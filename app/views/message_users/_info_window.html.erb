<div class= "mapboxgl-popup-content">
  <div class= "container">
    <div class= "profilepic">
    photo
      <%= image_tag "#{user_message.message.user.first_name}_#{user_message.message.user.last_name}.png" %>
    </div>
    <div class= "name">
    nom
      <%= "#{user_message.message.user.first_name} #{user_message.message.user.last_name}" %>
    </div>
    <div class="address">
    address
      <%= %>
    </div>
    <div class="hint">
    indice
      <%= %>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Supprimer</button>
        <button type="button" class="btn btn-primary">Itinéraire</button>
      </div>
  </div>
</div>
